
Ubuntu:

   all machines
      sudo apt-get install vim git htop python3 python3-pip sshfs curl
   desktop
      sudo apt-get install terminator xmonad dmenu nethogs gedit git scrot clusterssh gpick gimp
      #dmenu <--- trayer xmonad thingy
      misc optional desktop
         virtualbox openjdk-9-jdk

   dev linode
      sudo apt install tmux
      misc optional server
         openjdk-9-jdk
   server linode
      sudo apt install nginx #nginx-extras for perl

vim:
   NERDTree


arch:
   TODO

pip3 install --upgrade pip
sudo pip3 install #upgrade pip
sudo pip3 install pyinotify #for pywatch
sudo pip3 install sh #for vivie
